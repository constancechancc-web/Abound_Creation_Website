import Image from "next/image";
import Link from "next/link";
import type { Project } from "@/types/project";
export function ProjectCard({project,index}:{project:Project;index:number}){return <article className={`project-${index+1} group`}><Link href={`/work/${project.slug}`} className="block"><div className="relative aspect-[4/3] overflow-hidden bg-brand-light group-[.project-2]:aspect-[4/5] group-[.project-4]:aspect-[16/8]"><Image src={project.coverImage} alt={project.gallery[0].alt} fill sizes="(max-width:768px) 100vw, 70vw" className="object-cover transition-transform duration-700 ease-[var(--ease-out-expo)] group-hover:scale-[1.04]"/></div><div className="grid grid-cols-[auto_1fr_auto] gap-5 border-t border-black pt-3 text-xs uppercase tracking-[.08em]"><span>{String(index+1).padStart(2,"0")}</span><div><h3 className="text-base font-bold normal-case tracking-[-.03em] md:text-xl">{project.title}</h3><p className="mt-1 text-brand-gray group-hover:text-brand-red">{project.category}</p></div><span>{project.year} ↗</span></div></Link></article>}
